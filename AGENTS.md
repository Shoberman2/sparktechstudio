# SparkTech Studios — agent instructions

**The instructions for this repo live in [`CLAUDE.md`](./CLAUDE.md). Read that file.**
Design rules live in [`DESIGN.md`](./DESIGN.md); read it before any visual or UI change.

This file used to be a full copy of `CLAUDE.md`. The copy silently went stale —
it still described the studio's pre-pivot positioning ("we build the crazy ideas")
long after the site had moved to the AI + talent thesis, so any agent reading it
worked from the wrong brief. It is a pointer now so that cannot happen again.
Do not paste the instructions back in here.
